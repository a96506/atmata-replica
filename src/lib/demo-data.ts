/**
 * @deprecated Import from "@/mocks" or, preferably, from "@/lib/api/<resource>"
 * once each resource is wired through the fake service. This barrel exists only
 * to keep existing component imports working during the F0 → F1 migration.
 */
export * from "@/mocks";
